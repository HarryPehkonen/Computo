# Commits whose changes are formatting-only. `git blame` skips them, so blame keeps
# pointing at the commit that changed the CODE, not at the one that re-indented it.
#
# Enable it in a clone (once per clone, like core.hooksPath):
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# (GitHub reads this file too, when "Blame ignore commits" is configured for the repo.)
#
# a589d2b — style: one-time mechanical clang-format pass over the 31 drifting sources
#           (2026-09-20). Proven formatting-only: for all 31 files, every token outside a
#           #include directive is byte-identical and in the same order as before, with
#           adjacent string literals merged (BreakStringLiterals) and #include directives
#           and using-declarations compared as multisets (SortIncludes,
#           SortUsingDeclarations). Evidence: log/kanban-t_7c46709f/item4-format.log.
a589d2b19a9f5e5cd01c33aa1c68d68148f9b288
